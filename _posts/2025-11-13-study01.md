---
layout: post
title: "First study"
date: 2025-11-13 22:37:00 +0900
categories: study   # 카테고리 (여러 개 가능)
# tags: [GitHub, Jekyll, Hamilton]  # 태그 (선택)
math: false          # 수식 필요하면 true
toc: false           # 왼쪽에 목차 보이고 싶으면 true
---
오늘은 어제 공부하던 것을 이어서 했다.  
(Probabilistic selection of indc... , 3시간 정도 했다.)

최근 며칠 간 Gemini와 함께 공부하고 있다.  
GPT, Claude와 바꿔가면서 쓰고 있는데,  
컨텍스트 토큰이 가장 큰 Gemini가 논문 여러 개를 넣고 돌릴 때  
가장 안정적인 것 같다. 

방법 구현을 위해서도 생성형을 많이 쓰는 편인데,  
이전과 다르게 추론 시간이 길어질수록  
쓸데없는 예외처리를 많이 삽입한다.

```python
# 예시)
if not os.path.exists("data.csv"):
    print("data.csv not found. Downloading sample...")
    url = "url_link"
    df = pd.read_csv(url)
    print("Using downloaded sample 'data.csv'.")
else:
    df = pd.read_csv("data.csv")
    print("Using local 'data.csv'.")

if "X3" in df.columns:
    X = df.drop(columns=["X1", "X2", "X3"]).to_numpy(dtype=float)
else:
    X = df.drop(columns=["X1", "X2"]).to_numpy(dtype=float)
```
열받는다.  

내일은 더 열심히 해보자..